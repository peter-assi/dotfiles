[user]
	name = peterlind
	email = peter@lind.be
[alias]
        co = checkout
        stat = status
        st = status
        ci = commit
        df = !git diff | vim -
        lg = !git log | vim -

